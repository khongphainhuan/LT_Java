{
  "name": "queue-management-frontend",
  "version": "1.0.0",
  "description": "Frontend for Queue Management System - Staff & Admin Interface",
  "main": "src/js/auth.js",
  "scripts": {
    "dev": "live-server src/",
    "build": "npm run build:css && npm run build:js",
    "build:css": "postcss src/css/*.css -o dist/css/bundle.css --map",
    "build:js": "webpack --mode=production",
    "test": "jest",
    "lint": "eslint src/js/**/*.js"
  },
  "keywords": ["queue-management", "bootstrap", "chartjs", "thymeleaf"],
  "author": "Linh - Frontend Developer",
  "license": "MIT",
  "dependencies": {
    "bootstrap": "^5.1.3",
    "chart.js": "^4.0.0",
    "bootstrap-icons": "^1.7.2"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "eslint": "^8.0.0",
    "prettier": "^2.8.0"
  }
}